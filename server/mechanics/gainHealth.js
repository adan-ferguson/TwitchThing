// import scaledNumber from '../../game/scaledNumber.js'

export function gainHealth(combat, actor, scaling){
  // const gain = Math.ceil(scaledNumber(actor, scaling))
  // const hpBefore = actor.hp
  // actor.hp += gain
  // return {
  //   amount: actor.hp - hpBefore
  // }
}